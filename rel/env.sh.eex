#!/bin/sh

# Sets and samples the release environment variables.
# It is executed after the release is extracted.

# Configures the release cookie
# RELEASE_COOKIE="${RELEASE_COOKIE:-$(cat /app/releases/COOKIE)}"
